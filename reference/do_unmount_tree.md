# do_unmount_tree

Create a dataframe from a nested list.

## Usage

``` r
do_unmount_tree(base_tree, type)
```

## Arguments

- base_tree:

  nested list

- type:

  'variables' if nested list is refereed to variables, 'regions' if it
  refers to regional aggregation

## Value

dataframe
