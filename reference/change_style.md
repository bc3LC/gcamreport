# change_style

Change the style of the tree nodes: 'dis' if needs to be disabled,
'basic' otherwise.

## Usage

``` r
change_style(tree, type, tmp_vars = NULL)
```

## Arguments

- tree:

  base tree to change the style on

- type:

  either regions or variables

- tmp_vars:

  variables which require 'dis' style
