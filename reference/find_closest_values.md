# find_closest_values

Find the two closest values (one smaller and one bigger) of
values_vector to target_value.

## Usage

``` r
find_closest_values(values_vector, target_value)
```

## Arguments

- values_vector:

  Vector of values.

- target_value:

  Target value.

## Value

The two closest values (one smaller and one bigger) in one vector.
