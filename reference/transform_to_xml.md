# transform_to_xml

Converts a list of parsed queries into an XML document.

## Usage

``` r
transform_to_xml(parsed_queries_list)
```

## Arguments

- parsed_queries_list:

  List of parsed queries.

## Value

XML document generated from the provided queries list.
